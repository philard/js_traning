<!DOCTYPE html>
<html>
  <head>
    <title>Express Authorization Example - User Page</title>
  </head>
  <body>
    <h1>Express Authorization Example - Asset</h1>

    <p>You are currently logged in and are permitted to receive content authorized with the "noun:verb" permission.</p>

    <p>Click <a href="/logout">here to logout</a> or <a href="/">here to return to the home page</a>.
    </p>

    <form method="POST" action="/secure/user_page">
      <div>
        <label>Username</label>
        <input name="username" value=""/>
      </div>
      <div>
        <label>permissions with example</label>
        <input name="permissions" value="secure:view"/>
      </div>
      <input type="submit" value="Login" />
    </form>

  <table>
    <tbody>

      <tr>
        <th>resource / noun name</th><th>permission(s) / verb(s)</th>
      </tr>

      <%
      var permissions = user.permissions;
      for(var i=0; i<user.permissions.length; i++) { %>
      <tr>
        <td><%=user.permissions[i]%></td><td><%=user.permissions[i]%></td>
      </tr>
      <% } %>
    </tbody>
  </table>
  </body>
</html>
